// config.js — Production API override
//
// Leave this file empty for Render deployments (the server auto-detects the API URL).
//
// For Vercel (static-only) deployments, uncomment and set this to your
// Render chatbot-server URL so the frontend knows where to send API calls:
//
// window.OBSIDIAN_CONFIG = {
//   apiBase: 'https://YOUR-APP-NAME.onrender.com/api'
// };
